{
  "name": "oauth2-equivalent-nodejs",
  "version": "1.0.0",
  "description": "Replicating a complete OAuth 2.0 flow from scratch without hitting `npm install`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^6.3.0"
  }
}
